---
phase: 6
plan: 04
wave: 1
depends_on: []
files_modified: [skills/context-hygiene/SKILL.md, .claude/skills/context-hygiene/SKILL.md]
requirements_addressed: [LOCAL-04]
autonomous: true
---

# Plan 04: Context Hygiene Skill — Restrict Modifications to .claude/rules/local/ Only

<objective>
Restrict the hygiene-agent to only modify files in `.claude/rules/local/`. Committed rules in `.claude/rules/` and `CLAUDE.md` are read-only for analysis but never modified.
</objective>

<tasks>

<task id="1">
<title>Update context-hygiene SKILL.md with scope restriction</title>
<read_first>
- skills/context-hygiene/SKILL.md
</read_first>
<action>
Add a "Scope Restriction" section at the top of the skill (after the title):

```markdown
## Scope Restriction

**CRITICAL**: You may only CREATE or MODIFY files in `.claude/rules/local/`.
- `.claude/rules/*.md` (committed team rules): READ-ONLY — analyze but never modify
- `CLAUDE.md`: READ-ONLY — analyze but never modify
- `.claude/rules/local/*.md` (auto-generated rules): full read/write access
- Suggestion files in `.claude-auto-context/suggestions/`: create only
```

Update the Procedure section:
- Step 1: Read all `.claude/rules/*.md` files (committed team rules)
- Step 2: Read all `.claude/rules/local/*.md` files (auto-generated rules)
- Step 3: Read `CLAUDE.md`
- Remove H-05 (CLAUDE.md Bloat) — claudemd-agent is removed
- Change "6-Point Checklist" to "5-Point Checklist"
- Update suggestion naming to timestamp format: `YYYYMMDD-HHMMSS-hygiene-{slug}.md`
</action>
<acceptance_criteria>
- `grep -q 'Scope Restriction' skills/context-hygiene/SKILL.md` succeeds
- `grep -q 'READ-ONLY.*never modify' skills/context-hygiene/SKILL.md` succeeds
- `grep -q '\.claude/rules/local/' skills/context-hygiene/SKILL.md` succeeds
- `grep -q 'H-05' skills/context-hygiene/SKILL.md` returns failure
- `grep -q '5-Point' skills/context-hygiene/SKILL.md` succeeds
</acceptance_criteria>
</task>

<task id="2">
<title>Mirror changes to .claude/skills/context-hygiene/SKILL.md</title>
<read_first>
- .claude/skills/context-hygiene/SKILL.md
</read_first>
<action>
Copy the exact same content from `skills/context-hygiene/SKILL.md` (after task 1) to `.claude/skills/context-hygiene/SKILL.md`. Both files must be identical.
</action>
<acceptance_criteria>
- `diff skills/context-hygiene/SKILL.md .claude/skills/context-hygiene/SKILL.md` produces no output
</acceptance_criteria>
</task>

</tasks>

<verification>
1. "Scope Restriction" section present with READ-ONLY for committed rules and CLAUDE.md
2. H-05 removed, checklist is 5 points
3. Suggestion naming uses timestamp format
</verification>

<must_haves>
- Hygiene-agent cannot modify committed rules or CLAUDE.md
- Only .claude/rules/local/ files are writable
- H-05 (CLAUDE.md Bloat) removed
- Both skill file copies identical
</must_haves>
