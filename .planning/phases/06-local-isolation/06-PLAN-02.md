---
phase: 6
plan: 02
wave: 1
depends_on: []
files_modified: [skills/extract-rules/SKILL.md, .claude/skills/extract-rules/SKILL.md]
requirements_addressed: [LOCAL-01, LOCAL-02]
autonomous: true
---

# Plan 02: Extract Rules Skill — Output to .claude/rules/local/ + Global Rules

<objective>
Change the extract-rules skill to write auto-generated rules to `.claude/rules/local/` instead of `.claude/rules/`. Also document that rules without `globs:` frontmatter apply project-wide (replacing claudemd-agent's global knowledge role).
</objective>

<tasks>

<task id="1">
<title>Change output path from .claude/rules/ to .claude/rules/local/</title>
<read_first>
- skills/extract-rules/SKILL.md
</read_first>
<action>
In `skills/extract-rules/SKILL.md`:

1. Line 2: Change description from `into .claude/rules/ files` to `into .claude/rules/local/ files`
2. Line 8: Change `in .claude/rules/.` to `in .claude/rules/local/`.

Replace the Procedure section (lines 42-49) with:

```markdown
## Procedure

1. Read existing rules in `.claude/rules/local/` AND `.claude/rules/` to avoid duplication with both auto-generated and committed team rules
2. Analyze session data for repeated patterns
3. For each candidate:
   a. Verify 2+ sessions
   b. Verify NOT discoverable from code (use Glob/Read to check)
   c. Verify no duplication with existing rules (local or committed)
4. Write new rules files to `.claude/rules/local/`
5. Use narrow glob scoping (prefer `src/auth/**` over `**`)
6. For project-wide knowledge (not file-scoped), omit `globs:` from frontmatter — the rule will apply globally
```
</action>
<acceptance_criteria>
- `grep -c '\.claude/rules/local/' skills/extract-rules/SKILL.md` returns at least 3
- `grep -q 'omit.*globs.*frontmatter' skills/extract-rules/SKILL.md` succeeds
</acceptance_criteria>
</task>

<task id="2">
<title>Add global rules documentation section</title>
<read_first>
- skills/extract-rules/SKILL.md
</read_first>
<action>
After the "Output Format" section, add a new section:

```markdown
## Global vs Scoped Rules

- **Scoped rules**: Include `globs:` frontmatter to target specific file patterns
- **Global rules**: Omit `globs:` entirely — Claude applies these to ALL files project-wide
- Global rules replace CLAUDE.md additions for project-wide tacit knowledge
- Use global rules for: non-obvious build commands, dangerous naming traps, hidden dependencies
- Use scoped rules for: file-type conventions, directory-specific patterns
```
</action>
<acceptance_criteria>
- `grep -q '## Global vs Scoped Rules' skills/extract-rules/SKILL.md` succeeds
- `grep -q 'Omit.*globs.*entirely' skills/extract-rules/SKILL.md` succeeds
</acceptance_criteria>
</task>

<task id="3">
<title>Mirror changes to .claude/skills/extract-rules/SKILL.md</title>
<read_first>
- .claude/skills/extract-rules/SKILL.md
</read_first>
<action>
Copy the exact same content from `skills/extract-rules/SKILL.md` (after tasks 1-2) to `.claude/skills/extract-rules/SKILL.md`. Both files must be identical.
</action>
<acceptance_criteria>
- `diff skills/extract-rules/SKILL.md .claude/skills/extract-rules/SKILL.md` produces no output
</acceptance_criteria>
</task>

</tasks>

<verification>
1. `grep -c 'local/' skills/extract-rules/SKILL.md` returns 3+
2. No remaining references to writing directly to `.claude/rules/` (only reading for dedup)
3. Global rules section explains omitting globs for project-wide application
</verification>

<must_haves>
- All auto-generated rules written to .claude/rules/local/
- Dedup checks read both local/ and committed rules
- Global rules documented as globs-absent pattern
- Both skill file copies are identical
</must_haves>
